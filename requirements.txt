# ── Core / analysis (CPU, Tier-0 tests) ─────────────────────────────────────
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
pandas>=2.0
pyarrow>=14
pyyaml>=6.0
statsmodels>=0.14
pytest>=7.4

# ── Target model + activation extraction (GPU, Tier 1+) ──────────────────────
torch>=2.2
transformers>=4.45,<5      # kitft NLA recipe targets transformers 4.x
safetensors>=0.4
accelerate>=0.30
huggingface_hub>=0.24

# ── NLA inference client (vendored nla_inference.py needs these) ─────────────
httpx>=0.27
orjson>=3.9
# sglang[all]>=0.5.6   # installed in the Modal GPU image (heavy); not needed on CPU

# ── Orchestration ────────────────────────────────────────────────────────────
modal>=0.64
datasets>=2.18
